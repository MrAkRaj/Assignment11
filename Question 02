int HCF(int , int);

// function to calculate HCF of two numbers. (TSRS)

int HCF(int a , int b)
{
    for (int i=(a<b?a:b) ; i>=1 ; i--) 
    {
        if(a%i==0 && b%i==0)
        return i ;
    }
}
